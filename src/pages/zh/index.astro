---
import Layout from "../../layouts/Layout.astro";
import Home from "../../components/Home.astro";
import { getTranslation } from "../../i18n/translations";

const lang = "zh";
const t = getTranslation(lang);
---

<Layout lang={lang} t={t} page="">
  <Home lang={lang} t={t} />
</Layout>
